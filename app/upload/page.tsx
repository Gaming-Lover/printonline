import { requireUser } from "@/lib/auth";import { getPricing } from "@/lib/pricing";import { Card,CardContent,CardHeader,CardTitle,CardDescription } from "@/components/ui/card";import { UploadOrderForm } from "@/components/forms/upload-order-form";export default async function UploadPage(){await requireUser();const pricing=await getPricing();return <main className="container py-10"><Card><CardHeader><CardTitle>Upload and configure print job</CardTitle><CardDescription>PDF, JPG, JPEG, or PNG. Maximum size 50 MB.</CardDescription></CardHeader><CardContent><UploadOrderForm bwPrice={pricing.bwPrice} colorPrice={pricing.colorPrice}/></CardContent></Card></main>}
