import { getPricing } from "@/lib/pricing";import { requireAdmin } from "@/lib/auth";import { updatePricing } from "@/actions/admin";import { Card,CardContent,CardHeader,CardTitle,CardDescription } from "@/components/ui/card";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export default async function Pricing(){await requireAdmin();const p=await getPricing();return <main className="container max-w-xl py-10"><Card><CardHeader><CardTitle>Pricing manager</CardTitle><CardDescription>Update page prices instantly for new orders.</CardDescription></CardHeader><CardContent><form action={updatePricing} className="space-y-4"><div><Label>Black & White price (₹)</Label><Input name="bwPrice" type="number" min="1" defaultValue={p.bwPrice}/></div><div><Label>Color price (₹)</Label><Input name="colorPrice" type="number" min="1" defaultValue={p.colorPrice}/></div><Button>Save pricing</Button></form></CardContent></Card></main>}
