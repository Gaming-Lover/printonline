import { prisma } from "@/lib/prisma";import { requireAdmin } from "@/lib/auth";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";
export default async function Queue(){await requireAdmin();const jobs=await prisma.printJob.findMany({include:{order:{include:{user:{select:{email:true,name:true}}}}},orderBy:{createdAt:"asc"}});return <main className="container py-10"><Card><CardHeader><CardTitle>Print queue</CardTitle></CardHeader><CardContent><div className="grid gap-3">{jobs.map(j=><div key={j.id} className="flex items-center justify-between rounded-lg border p-4"><div><div className="font-medium">{j.order.fileName}</div><div className="text-sm text-muted-foreground">{j.order.user.email} · {j.order.pages} pages · {j.order.copies} copies</div></div><Badge>{j.status}</Badge></div>)}</div></CardContent></Card></main>}
