import Link from "next/link";import { prisma } from "@/lib/prisma";import { requireUser } from "@/lib/auth";import { Button } from "@/components/ui/button";import { Card,CardContent,CardHeader,CardTitle,CardDescription } from "@/components/ui/card";import { OrdersTable } from "@/components/orders-table";
export default async function Dashboard(){const user=await requireUser();const orders=await prisma.order.findMany({where:{userId:user.id},orderBy:{createdAt:"desc"},take:20,include:{payment:true,printJob:true}});return <main className="container py-10"><div className="mb-6 flex items-center justify-between"><div><h1 className="text-3xl font-bold">Your orders</h1><p className="text-muted-foreground">Track payment and print status.</p></div><Button asChild><Link href="/upload">New print job</Link></Button></div><Card><CardHeader><CardTitle>Order history</CardTitle><CardDescription>Latest 20 jobs. Use API pagination for larger history.</CardDescription></CardHeader><CardContent><OrdersTable orders={orders}/></CardContent></Card></main>}
