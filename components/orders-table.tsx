import Link from "next/link";import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table";import { Badge } from "@/components/ui/badge";import { formatCurrency } from "@/lib/utils";
export function OrdersTable({orders,admin=false}:{orders:any[];admin?:boolean}){return <Table><TableHeader><TableRow><TableHead>Order</TableHead>{admin&&<TableHead>User</TableHead>}<TableHead>File</TableHead><TableHead>Amount</TableHead><TableHead>Payment</TableHead><TableHead>Status</TableHead><TableHead></TableHead></TableRow></TableHeader><TableBody>{orders.map(o=><TableRow key={o.id}><TableCell className="font-mono text-xs">{o.id}</TableCell>{admin&&<TableCell>{o.user?.email}</TableCell>}<TableCell>{o.fileName}</TableCell><TableCell>{formatCurrency(o.amount)}</TableCell><TableCell><Badge variant={o.paymentStatus==="PAID"?"default":"secondary"}>{o.paymentStatus}</Badge></TableCell><TableCell><Badge variant="outline">{o.orderStatus}</Badge></TableCell><TableCell><Link className="text-primary" href={admin?`/admin/orders?order=${o.id}`:`/orders/${o.id}`}>View</Link></TableCell></TableRow>)}</TableBody></Table>}
