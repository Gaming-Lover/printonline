import Link from "next/link";import { getSession,destroySession } from "@/lib/auth";import { Button } from "@/components/ui/button";import { Printer } from "lucide-react";
async function logout(){"use server";await destroySession();}
export async function Nav(){const user=await getSession();return <header className="sticky top-0 z-40 border-b bg-background/80 backdrop-blur"><div className="container flex h-16 items-center justify-between"><Link href="/" className="flex items-center gap-2 font-bold"><Printer className="h-6 w-6 text-primary"/>PrintHub</Link><nav className="flex items-center gap-2 text-sm"><Link href="/upload" className="hidden sm:block hover:text-primary">Upload</Link>{user?<><Link href={user.role==="ADMIN"?"/admin":"/dashboard"} className="hover:text-primary">Dashboard</Link><form action={logout}><Button variant="outline" size="sm">Logout</Button></form></>:<><Button asChild variant="ghost" size="sm"><Link href="/login">Login</Link></Button><Button asChild variant="outline" size="sm"><Link href="/admin/login">Admin</Link></Button><Button asChild size="sm"><Link href="/register">Get started</Link></Button></>}</nav></div></header>}
